version: 2

build:
  image: "niklasheim/qadence-rtd:v0.1"
  commands:
    - pip3 install hatch
    - hatch -v run docs:build
    - mkdir _readthedocs/
    - mv site _readthedocs/html

conda:
  environment: docs/environment.yml

mkdocs:
  configuration: mkdocs.yml
